---
/**
 * PERFORMANCE: Google Fonts with preconnect for faster loading in Vietnam
 * Google Fonts CDN has servers in Singapore/Hong Kong - much closer than Netlify
 * Using font-display: swap via CSS &display=swap parameter
 */

// Font list for reference:
// 'DM Sans'
// Nunito
// Dosis
// Outfit
// Roboto
// Literata
// 'IBM Plex Sans'
// Karla
// Poppins
// 'Fira Sans'
// 'Libre Franklin'
// Inconsolata
// Raleway
// Oswald
// 'Space Grotesk'
// Urbanist
---

<!-- PERFORMANCE: Preconnect to Google Fonts (must be early in <head>) -->
<!-- These eliminate DNS lookup + TCP + TLS handshake time -->
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

<!-- Google Fonts: Inter with Vietnamese subset -->
<!-- display=swap ensures text is visible immediately with fallback font -->
<link 
  rel="stylesheet"
  href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap&subset=latin,vietnamese"
/>

<style is:inline>
  /* Font-display: swap is handled by Google Fonts URL parameter */
  
  :root {
    --aw-font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    --aw-font-serif: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    --aw-font-heading: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;

    --aw-color-primary: rgb(1 97 239);
    --aw-color-secondary: rgb(1 84 207);
    --aw-color-accent: rgb(109 40 217);

    --aw-color-text-heading: rgb(0 0 0);
    --aw-color-text-default: rgb(16 16 16);
    --aw-color-text-muted: rgb(16 16 16 / 66%);
    --aw-color-bg-page: rgb(255 255 255);

    --aw-color-bg-page-dark: rgb(3 6 32);

    ::selection {
      background-color: lavender;
    }
  }

  .dark {
    --aw-font-sans: 'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    --aw-font-serif: 'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    --aw-font-heading: 'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;

    --aw-color-primary: rgb(1 97 239);
    --aw-color-secondary: rgb(1 84 207);
    --aw-color-accent: rgb(109 40 217);

    --aw-color-text-heading: rgb(247, 248, 248);
    --aw-color-text-default: rgb(229 236 246);
    --aw-color-text-muted: rgb(229 236 246 / 66%);
    --aw-color-bg-page: rgb(3 6 32);

    ::selection {
      background-color: black;
      color: snow;
    }
  }

  /* Google Translate Integration Styles */
  /* Hide Google Translate banner and branding */
  .goog-te-banner-frame {
    display: none !important;
  }

  .goog-te-gadget {
    font-size: 0 !important;
  }

  .goog-te-gadget .goog-te-combo {
    margin: 0 !important;
  }

  .goog-te-menu-value span:first-child {
    display: none;
  }

  .goog-te-menu-value:before {
    content: '' !important;
  }

  /* Hide Google Translate footer */
  .goog-te-gadget-icon {
    display: none !important;
  }

  /* Ensure notranslate elements are protected from translation */
  .notranslate {
    -webkit-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
    user-select: none;
  }

  [translate="no"] {
    -webkit-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
    user-select: none;
  }

  /* User content styling for Google Translate */
  .user-content {
    /* Allow Google Translate to work on these elements */
  }

  [data-user-content] {
    /* Allow Google Translate to work on these elements */
  }
</style>

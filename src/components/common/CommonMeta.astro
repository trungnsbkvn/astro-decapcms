---
import { getAsset } from '~/utils/blog-permalinks';
---

<meta charSet="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />

<link rel="sitemap" href={getAsset('/sitemap-index.xml')} />

<!-- 
  Preconnect only to domains we actually use immediately.
  dns-prefetch for deferred/lazy-loaded resources.
  No Google Fonts - we use locally bundled @fontsource-variable/inter
-->
<link rel="dns-prefetch" href="https://embed.tawk.to" />
<link rel="dns-prefetch" href="https://www.googletagmanager.com" />

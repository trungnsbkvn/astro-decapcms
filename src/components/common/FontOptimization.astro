---
/**
 * Font and CSS Optimization Component
 * - Uses Google Fonts CDN (faster in Vietnam - Singapore/HK servers)
 * - Implements font-display: swap via Google Fonts URL parameter
 * - Critical font styles inlined for faster rendering
 * - System font fallback prevents FOIT (Flash of Invisible Text)
 */
---

<!-- Critical font CSS - optimized for Google Fonts Inter -->
<style is:inline>
  /* Critical font optimization */
  html {
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    /* PERFORMANCE: Use optimizeSpeed for faster text rendering */
    text-rendering: optimizeSpeed;
  }

  /* Font stack with Inter from Google Fonts */
  body {
    /* PERFORMANCE: System font fallback stack prevents FOIT */
    font-family: 'Inter', ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    font-feature-settings: 'kern' 1;
    font-size: 16px;
    line-height: 1.5;
    /* PERFORMANCE: Enable font optimization hints */
    font-synthesis: none;
    text-size-adjust: 100%;
  }

  /* Hero text optimization */
  h1, h2, h3 {
    font-family: inherit;
    font-weight: 600;
    /* PERFORMANCE: Use optimizeSpeed for headings too */
    text-rendering: optimizeSpeed;
  }

  /* Reduce CLS by declaring font sizes with contain */
  h1 {
    font-size: clamp(2rem, 5vw, 3rem);
    line-height: 1.2;
    /* PERFORMANCE: Contain layout to prevent reflows */
    contain: layout style;
  }

  h2 {
    font-size: clamp(1.5rem, 4vw, 2.25rem);
    line-height: 1.3;
    contain: layout style;
  }

  h3 {
    font-size: clamp(1.25rem, 3vw, 1.875rem);
    line-height: 1.4;
    contain: layout style;
  }

  p {
    font-size: 1rem;
    line-height: 1.6;
  }

  /* Touch performance */
  a, button {
    touch-action: manipulation;
  }
  
  button {
    cursor: pointer;
  }
  
  /* PERFORMANCE: Contain paint for interactive elements */
  .btn, button, a {
    contain: layout style;
  }
</style>

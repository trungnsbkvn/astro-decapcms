---
/**
 * Font and CSS Optimization Component
 * - Preloads critical fonts with font-display: swap
 * - Implements font subsetting for better performance
 * - Manages system fonts fallback
 */
---

<!-- Fonts CSS with font-display: swap (from CDN) -->
<link 
  rel="stylesheet" 
  href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap"
/>

<!-- Fallback for no-JS -->
<noscript>
  <link 
    rel="stylesheet" 
    href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" 
  />
</noscript>

<!-- Inline critical CSS for above-the-fold -->
<style>
  /* Critical font-face with swap - using system fonts instead of custom files */
  html {
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
  }

  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    font-feature-settings: 'kern' 1;
  }

  /* Hero text optimization */
  h1, h2, h3 {
    font-family: inherit;
    font-weight: 600;
    text-rendering: geometricPrecision;
  }
</style>

<!-- Reduce CLS by declaring font sizes -->
<style>
  /* Prevent layout shift for text elements */
  body {
    font-size: 16px;
    line-height: 1.5;
  }

  h1 {
    font-size: clamp(2rem, 5vw, 3rem);
    line-height: 1.2;
  }

  h2 {
    font-size: clamp(1.5rem, 4vw, 2.25rem);
    line-height: 1.3;
  }

  h3 {
    font-size: clamp(1.25rem, 3vw, 1.875rem);
    line-height: 1.4;
  }

  p {
    font-size: 1rem;
    line-height: 1.6;
  }

  /* Link performance */
  a {
    touch-action: manipulation;
  }

  /* Button performance */
  button {
    touch-action: manipulation;
    cursor: pointer;
  }
</style>
